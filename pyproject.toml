[project]
name = "job-tracker"
version = "0.1.0"
description = "Istanbul / remote DS-ML-AI job listings aggregator"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "httpx>=0.27",
    "pyyaml>=6.0",
    "python-multipart>=0.0.9",
    "selectolax>=0.3.21",
    "uvicorn>=0.30",
]

[project.optional-dependencies]
browser = ["playwright>=1.44"]

[tool.uv]
package = false
